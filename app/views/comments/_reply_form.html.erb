<div class="postConstraint subPost form subCommentForm" id="commentForm_<%= @commentable.id %>">
  <div class="postUser">
    <div class="formField">
      <%= label_tag '', '&nbsp;'.html_safe %><br/>
      <%= link_to image_tag('default_user.png', class: 'userImageMed'), current_user %>
      <span class="postUsername"><%= link_to @commentable.user.username %></span>
    </div>
  </div>

  <%= form_for [@commentable, @comment], url: comment_reply_path(@commentable) do |f| %>
    <div class="formField">
      <%= f.label :message, "Reply to #{@commentable.user.username}" %><br/>
      <%= f.text_area :message %>
    </div>
    
    <div class="formSubmit">
      <%= f.submit 'Add Comment' %>
    </div>
  <% end %>
</div>